function add () {
    let a = prompt('Введите число');
    let b = prompt('Введите число');
    alert(Number(a) + Number(b))
}

function subtract () {
    let a = prompt('Введите число');
    let b = prompt('Введите число');
    alert(Number(a) - Number(b))
}

function divide () {
    let a = prompt('Введите число');
    let b = prompt('Введите число');
    alert(Number(a) / Number(b))
}

function multiply () {
    let a = prompt('Введите число');
    let b = prompt('Введите число');
    alert(Number(a) * Number(b))
}